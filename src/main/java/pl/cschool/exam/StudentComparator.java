package pl.cschool.exam;

import java.util.Comparator;
import java.util.Map;

public class StudentComparator implements Comparator<Map<Student, Double>> {

    @Override
    public int compare(Map<Student, Double> o1, Map<Student, Double> o2) {
        return 0 ;
    }
}
